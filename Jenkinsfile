pipeline {
    stages {
        stage('One') { 
            steps {
                echo 'Tset: npm install' 
            }
        }
        stage('Build') { 
            steps {
                sh 'npm install' 
            }
        }
    }
}